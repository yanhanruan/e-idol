{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@src/*": ["src/*"],
      "@assets/*": ["src/assets/*"]
      // (如果 assets 在根目录，则使用 ["assets/*"])
    }
  },
  "exclude": ["node_modules", "dist"]
}